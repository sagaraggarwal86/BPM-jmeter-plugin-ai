# CHANGED (G-03): New file — JMeter Plugins Manager integration descriptor.
# Picked up automatically from the JAR classpath by the Plugins Manager.
# Declares the plugin identity, version, and its dependency on the WebDriver Sampler plugin.

plugin.id=bpm-jmeter-plugin
plugin.name=Browser Performance Metrics
plugin.description=JMeter listener plugin that captures browser rendering metrics (Core Web Vitals, network, runtime, console errors) from WebDriver Sampler executions via Chrome DevTools Protocol. Provides a live results table with SLA-based highlighting, a composite performance score, bottleneck detection, and a CI-friendly bpm-summary.json output.
plugin.version=0.1.0
plugin.versions.max=5.6.3
plugin.installed.dependencies=jpgc-webdriver